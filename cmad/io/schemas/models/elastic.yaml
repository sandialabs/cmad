$schema: https://json-schema.org/draft/2020-12/schema
type: object
required: [name, def_type]
additionalProperties: false
properties:
  name:
    const: elastic
  def_type:
    enum: [full_3d, plane_stress, uniaxial_stress]
  elastic_stress:
    enum: [isotropic_linear, neohookean]
